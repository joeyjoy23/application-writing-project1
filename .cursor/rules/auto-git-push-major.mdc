---
description: 完成任务后自动 git commit 并 push 到 Cloud
alwaysApply: true
---

# 自动提交与推送（Streamlit Cloud）

用户要求：**每次**完成代码相关任务后，Agent **应主动** `git commit` 并 `git push`，无需再口头要求。

## 何时提交

- 修 bug、新功能、UI/工作流/测试/文档等**已落地的源码改动**
- 已通过相关验证（改 Python/UI/工作流后跑 `pytest`，或用户要求的检查）
- 纯问答、只读审查、无文件改动：**不**提交

不算需提交：未验证的 WIP、用户当次明确说「先别提交」。

## 提交前

1. 并行查看：`git status`、`git diff`、`git log -3`
2. **不要**提交：`logs/`、`.env`、密钥、宣传 docx（`docs/*宣传*.docx`）
3. 写 1–2 句 commit message，说明「为什么」

## 提交后

1. `git push` 到当前跟踪分支（禁止 force push main/master）
2. 网络失败时重试数次（443 超时常见）
3. 向用户简要报告 commit hash 与 push 结果；提醒 Streamlit Cloud **Reboot**（若有部署影响）

## 冲突

与用户**当次**「不要提交」指令冲突时，以当次指令为准。
