---
description: 从应用文 HTML/Word 导出生成课堂 PPT（V1 python-pptx + 四阶段页型）。用户提到课件、PPT、幻灯片、出课件、做PPT、一键课件、导出做演示时使用。
alwaysApply: false
---

# 应用文导出 → 课件

1. **读取 skill**：`.cursor/skills/yingyongwen-export-to-ppt/SKILL.md` + `SLIDE_BLUEPRINT.md`（含 **教室字号 ≥26pt**、**范文三段式 + 首行缩进**）。

2. **默认 V1**：`one_click_classroom_ppt.py` → `generate_classroom_pptx.py` + `pptx_click_reveal.py`（WPS on-click fade）。Stage3 来自 `stage3.json` 表格页。默认 **`--preset 70min`**（`40min`/`80min` 可选）。

3. **固定页型 + Stage4 融入主流程**（非附录）；范文 **Dear James + 开篇/主体/结尾** 三段，正文 **首行缩进**。

4. 先 `prepare_ppt_source.py`；**默认 Architecture V1**（分析报告为唯一结构源）。`-课件.html` 仅作输入别名（自动解析 sibling 分析 HTML）；旧 16 页骨架需 `one_click --classroom-html`。可选 Agent 撰写 `classroom_deck.json`（Stage3 页序 + layout）。

勿提交 `exports/`、临时 `ppt-work/` 到应用文 git 仓库。
