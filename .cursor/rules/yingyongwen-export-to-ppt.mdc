---
description: 从应用文 HTML/Word 导出生成课堂 PPT（V1 python-pptx + 四阶段页型）。用户提到课件、PPT、幻灯片、出课件、做PPT、一键课件、导出做演示时使用。
alwaysApply: false
---

# 应用文导出 → 课件

1. **读取 skill**：`.cursor/skills/yingyongwen-export-to-ppt/SKILL.md` + `known-issues.md`（WPS 动画踩坑）。

2. **默认 V2**：`one_click_classroom_ppt.py --v2` → `generate_classroom_pptx_v2.py` + `classroom_script` + `pptx_click_reveal.py`（**禁止** `cNvPr hidden="1"`）。Stage3 来自 `stage3.json`；句型页序 body→改一句→本题。默认 **`--preset 70min`**。

3. **固定页型 + Stage4 融入主流程**（非附录）；范文 **Dear James + 开篇/主体/结尾** 三段，正文 **首行缩进**。

4. 先 `prepare_ppt_source.py`；**Architecture V1** + `classroom_script` 模板。用户改流程后让其更新 skill 快照。

勿提交 `exports/`、临时 `ppt-work/` 到应用文 git 仓库。
